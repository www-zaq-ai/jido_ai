# Jido.AI environment variables for local development and examples.
#
# These are read by `.env` loading in `mix jido_ai` and example scripts.
# API keys are typically wired in your app config (see README `config :req_llm`).

# ---------------------------------------------------------------------------
# Core defaults used by Jido.AI model aliases (`lib/jido_ai.ex`)
# ---------------------------------------------------------------------------

# Default text/reasoning aliases (`:fast`, `:capable`, `:thinking`, etc.)
ANTHROPIC_API_KEY=

# Default image + embedding aliases (`:image`, `:embedding`)
OPENAI_API_KEY=

# Optional model override for opt-in live LLM smoke tests.
JIDO_LIVE_MODEL=openai:gpt-4o-mini

# ---------------------------------------------------------------------------
# Example-specific keys
# ---------------------------------------------------------------------------

# Needed by browser demo/search tools (`lib/examples/scripts/browser_demo.exs`)
BRAVE_SEARCH_API_KEY=

# Needed by issue triage agent (`lib/examples/agents/issue_triage_agent.ex`)
GITHUB_TOKEN=

# ---------------------------------------------------------------------------
# Optional provider keys (not required for default examples)
# ---------------------------------------------------------------------------

OPENROUTER_API_KEY=
GOOGLE_API_KEY=
XAI_API_KEY=
GROQ_API_KEY=
CEREBRAS_API_KEY=
ZAI_API_KEY=
